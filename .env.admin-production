# Loaded by `yarn build:admin` (vite build --mode admin-production).
# Use for a deployment that only serves admin.daladan.uz, or to match CI job name.
VITE_API_BASE_URL=https://api.daladan.uz/api/v1
VITE_ADMIN_APP_HOSTS=admin.daladan.uz
