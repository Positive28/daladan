# Loaded by `yarn build` (vite build, mode production).
# Same artifact can serve both www/main and admin: runtime hostname picks marketplace vs admin UI.
VITE_API_BASE_URL=https://api.daladan.uz/api/v1
VITE_ADMIN_APP_HOSTS=admin.daladan.uz
