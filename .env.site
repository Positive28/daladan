# Loaded by `yarn build:site` (vite build --mode site).
# Marketplace-only build: no admin hostnames baked in. Use if you deploy the main site separately
# and point admin.daladan.uz at another project that runs `yarn build:admin`.
VITE_API_BASE_URL=https://api.daladan.uz/api/v1
